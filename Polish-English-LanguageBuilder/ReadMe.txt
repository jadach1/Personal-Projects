If using on linux you need to have the -std=c++11 over above turned "on"

Upon loading this program will need access to the "words" text file

There are still some bugs with the inpupt methods for example entering a '~' into the prompt will cause a crash

Was trying to get the authentic Polish characters to appear on screen but the windows compiler does not recognize anything above ASCII characters, maybe in the future I can find a way around